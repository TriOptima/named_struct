============
Installation
============

At the command line::

    $ pip install tri.named_struct

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tri.named_struct
    $ pip install tri.named_struct
